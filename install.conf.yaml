- defaults:
    link:
        relink: true

- clean: ['~']

- link:
    ~/.gitconfig:
    ~/.tmux:
    ~/.tmux.conf:
    ~/.tmux.conf.linux:
    ~/.tmux.conf.osx:
    ~/.bash_profile:
    ~/.config/powerline: config/powerline

- shell: []


